# Automic JSON fixtures

Synthetic responses modelled from Broadcom AE REST docs. **Not verified against a
live Automic instance.** Once the `--record` mode from Phase 5 captures real
responses, replace these with the real shapes and update per-kind normalizers
accordingly.

See `docs/ISSUES.md` — "Automic JSON field shape" for the verification task.
